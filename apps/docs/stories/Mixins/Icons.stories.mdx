import { Meta } from '@storybook/addon-docs/blocks';
import { IconGallery, IconItem } from '@storybook/addon-docs';
import { Icons } from 'ui';

<Meta title='Design Token/Icons' />

# Icons

<IconGallery>
  {Object.entries(Icons).map(([name, IconComponent]) => (
    <IconItem name={name}>
      <IconComponent />
    </IconItem>
  ))}
</IconGallery>
